<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Person Registration</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        /* Additional styles for edit/delete buttons */
        .btn-group {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-edit {
            background: #3498db;
            color: white;
        }

        .btn-edit:hover {
            background: #2980b9;
            transform: translateY(-2px);
        }

        .btn-delete {
            background: #e74c3c;
            color: white;
        }

        .btn-delete:hover {
            background: #c0392b;
            transform: translateY(-2px);
        }

        .btn-cancel {
            background: #95a5a6;
            color: white;
        }

        .btn-cancel:hover {
            background: #7f8c8d;
        }

        .btn-update {
            background: #27ae60;
            color: white;
        }

        .btn-update:hover {
            background: #229954;
        }

        .edit-form {
            margin-top: 15px;
            padding: 15px;
            background: rgba(52, 152, 219, 0.05);
            border-radius: 5px;
            border: 1px solid #3498db;
        }

        .edit-form .form-group {
            margin-bottom: 10px;
        }

        .edit-form input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .record-card.editing {
            background: rgba(52, 152, 219, 0.1) !important;
            border: 2px solid #3498db !important;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Alaska DOH - Person Registration Demo (SSR)</h1>

        <div class="form-section">
            <h2>Register New Person</h2>
            <form method="post" action="/persons">
                <div class="form-group">
                    <label for="name">Name:</label>
                    <input type="text" id="name" name="name" required>
                </div>
                <div class="form-group">
                    <label for="address">Address:</label>
                    <input type="text" id="address" name="address" required>
                </div>
                <button type="submit" class="btn-submit">Submit</button>
            </form>
        </div>

        <div class="records-section">
            <h2>Registered Persons</h2>
            <div class="records-list">
                {{#persons}}
                <div class="record-card">
                    <div class="record-id">ID: {{id}}</div>
                    <div class="record-field">
                        <span class="field-label">Name:</span>
                        <span class="field-value">{{name}}</span>
                    </div>
                    <div class="record-field">
                        <span class="field-label">Address:</span>
                        <span class="field-value">{{address}}</span>
                    </div>
                    <div class="btn-group">
                        <a href="/persons?editId={{id}}" class="btn btn-edit">Edit</a>
                        <form method="post" action="/persons/delete/{{id}}" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this person?');">
                            <button type="submit" class="btn btn-delete">Delete</button>
                        </form>
                    </div>
                </div>
                {{/persons}}
                {{^persons}}
                <p class="no-records">No persons registered yet. Use the form above to add the first person.</p>
                {{/persons}}

                {{#editPerson}}
                <div class="record-card editing" style="margin-top: 20px;">
                    <div class="record-id">Editing ID: {{id}}</div>
                    <div class="edit-form">
                        <form method="post" action="/persons/update">
                            <input type="hidden" name="id" value="{{id}}">
                            <div class="form-group">
                                <label>Name:</label>
                                <input type="text" name="name" value="{{name}}" required>
                            </div>
                            <div class="form-group">
                                <label>Address:</label>
                                <input type="text" name="address" value="{{address}}" required>
                            </div>
                            <div class="btn-group">
                                <button type="submit" class="btn btn-update">Update</button>
                                <a href="/persons" class="btn btn-cancel">Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
                {{/editPerson}}
            </div>
        </div>
    </div>
</body>
</html>
